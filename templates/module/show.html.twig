{% extends 'base.html.twig' %}

{% block title %}Module{% endblock %}

{% block body %}
    <h1>Module</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ module.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ module.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ module.type }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ module.status }}</td>
            </tr>
            <tr>
                <th>LastMesuredValue</th>
                <td>{{ module.lastMesuredValue }}</td>
            </tr>
            <tr>
                <th>NumberDataSent</th>
                <td>{{ module.NumberDataSent }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_module_index') }}">back to list</a>

    <a href="{{ path('app_module_edit', {'id': module.id}) }}">edit</a>

    {{ include('module/_delete_form.html.twig') }}
{% endblock %}
